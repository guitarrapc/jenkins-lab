credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          scope: GLOBAL
          id: "github"
          description: "github token"
          password: ${github:-githubPassword} # from docker-compose
          username: "guitarrapc"
